// program to calculate simple intrest 
#include 
main ()
{float pa, roi, si;
printf(enter principle amount, rate of intrest and time)
scanf("%f%f%f" &pa,&roi,&time);
si= (pa*roi*time*)/100;
printf("\n calculate simple intrest for principle amount %f rate of intrest %f time %f is %f", pa, roi, time, si,);

}
